#!/usr/bin/python3

""" reads a text file """

def write_file(filename="",text=""):
    """ reads a file """
    with open(filename, encoding='utf-8', mode='w') as file:
        return file.write(text)
